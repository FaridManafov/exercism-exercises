//
// This is only a SKELETON file for the 'Two fer' exercise. It's been provided as a
// convenience to get you started writing code faster.
//

export const twoFer = (name) => {
  let string = `One for ${name ? name : "you"}, one for me.`
  
  if (name === undefined) {
    return string
  }
  
  else {
    return string
  }
  
};
